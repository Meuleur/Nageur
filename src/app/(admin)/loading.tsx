import { SqueletteEcran } from "@/components/ui/squelette";

/** Frontière Suspense du groupe — squelette homogène (B4). */
export default function GroupeLoading() {
  return <SqueletteEcran large />;
}
